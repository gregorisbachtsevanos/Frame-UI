# `@frame-ui/theme`

Theme package used by UI and Storybook.

## Build from this folder

```bash
cd packages/theme
pnpm build
```

## Same command from repo root

```bash
pnpm --filter @frame-ui/theme build
```
